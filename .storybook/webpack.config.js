module.exports = {
  resolve: {
    alias: {
      client: `${__dirname}/../imports/client`,
      server: `${__dirname}/../imports/server`,
      utils: `${__dirname}/../imports/utils`,
    },
  },
};
